# secbot-ai 系统介绍

> 对话式多智能体网络安全协作平台 — 让安全运营像对话一样自然

---

## 一、系统简介

secbot-ai（项目代号 VAPT3 / nanobot）是一个面向网络安全运营的**对话式多智能体 VAPT 平台**（Vulnerability Assessment & Penetration Testing）。它将大语言模型的推理能力与专业安全工具深度结合，通过自然语言对话驱动完整的安全测试流水线。

传统的安全评估工作往往需要安全工程师在多个工具之间频繁切换，手动编写扫描命令、整理结果、撰写报告，流程冗长且对经验依赖极高。secbot-ai 从根本上改变了这一模式：用户只需用自然语言描述安全需求——例如"扫描 192.168.1.0/24 网段的高危漏洞并生成报告"——系统即可自动理解意图、规划任务、编排多个专家智能体并行协作，最终交付结构化的安全评估报告。

系统的核心设计理念是**"对话即调度"**。主控智能体（Orchestrator）利用 LLM 的 Function Calling 能力进行动态规划，无需预先绘制流程图或硬编码工作流。每个安全能力被封装为独立的专家智能体，拥有专属的提示词、工具集和输入输出规范，彼此完全解耦。新增一种安全能力只需添加一条 YAML 配置，系统即可自动将其纳入编排候选池。

在安全合规层面，系统内置了多层防护机制：高危操作（如漏洞利用、弱口令爆破）执行前必须经过人工确认，所有工具调用均有完整的审计日志，SSRF 防护和工作区沙箱确保智能体行为始终在可控范围内。

---

## 二、系统架构

secbot-ai 采用分层架构设计，从上到下分为用户接入层、通道层、消息总线、核心引擎、编排层、专家智能体层、技能系统、持久化层和工作流引擎九大模块，各层职责清晰、松耦合高内聚。

### 2.1 架构总览

```
┌─────────────────────────── 用户接入层 ───────────────────────────┐
│  WebUI (React)  ·  CLI  ·  Telegram  ·  Discord  ·  Slack        │
│  钉钉  ·  飞书  ·  QQ  ·  OpenAI 兼容 API                        │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│  通道层 — ChannelManager 多通道路由中枢                            │
│  WebSocket · 插件式通道发现 · 流式 delta 合并 · 重复抑制            │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│  消息总线 — MessageBus (asyncio.Queue 双队列)                      │
│  Inbound Queue（入站）  ←→  Outbound Queue（出站）                 │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│  核心引擎 — AgentLoop + AgentRunner                                │
│  上下文治理 6 步管线 · 会话管理 · Provider 热重载 · 上下文压缩       │
└────────────┬───────────────────────────────┬─────────────────────┘
             │                               │
┌────────────▼──────────┐        ┌───────────▼────────────────────┐
│  编排层                │        │  技能系统                       │
│  Orchestrator 提示词   │        │  28+ 安全技能                   │
│  AgentRegistry (YAML)  │        │  SkillTool 执行链               │
│  SubagentManager       │        │  HighRiskGate 高危门控          │
│  HighRiskGate          │        │  SkillsLoader 加载器            │
└────────────┬──────────┘        └───────────┬────────────────────┘
             │                               │
┌────────────▼───────────────────────────────▼────────────────────┐
│  专家智能体层                                                     │
│  资产发现 · 端口扫描 · Web 爬取 · 漏洞预筛                         │
│  漏洞扫描 · 弱口令检测 · 报告生成                                  │
└────────────────────────────┬─────────────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────────────┐
│  持久化层 — CMDB (SQLite + SQLAlchemy + Alembic)                  │
│  Scan · Asset · Service · Vulnerability · Report                  │
└──────────────────────────────────────────────────────────────────┘
```

### 2.2 核心引擎

核心引擎由 **AgentLoop**（产品层处理引擎，约 1737 行）和 **AgentRunner**（LLM 工具循环引擎）两层构成。

- **AgentLoop** 负责会话管理、工具注册、通道协调和子智能体编排。它维护着会话持久化（SessionManager）、黑板共享（BlackboardRegistry）、资产推送（AssetFeedRegistry）、高危门控（HighRiskGate）等组件，是整个系统运转的中枢。
- **AgentRunner** 负责纯粹的"LLM 调用 → 工具执行 → 结果注入"核心循环。每次迭代前执行 6 步上下文治理管线（孤立工具结果清理 → 缺失结果补缺 → 旧结果摘要化 → 超大结果截断 → token 预算裁剪 → 二次清理），确保长对话场景下上下文窗口的有效利用。

两层之间通过 `AgentRunSpec`（执行配置）和 `AgentRunResult`（执行结果）进行数据传递，职责边界清晰。

### 2.3 编排与子智能体协作

编排层是系统的"大脑"，由三个核心组件协同工作：

1. **Orchestrator**（编排中枢）：纯提示词模块，通过四段锁定结构（角色定义 → 11 条硬规则 → 动态专家表 → 工作风格）生成系统提示词，指导 LLM 进行任务规划和智能体调度。
2. **AgentRegistry**（专家注册表）：YAML 驱动的全量验证机制，启动时扫描所有 `agents/*.yaml` 文件，逐一验证名称格式、技能互斥、提示词文件存在性、JSON Schema 合规性和二进制可用性，任何验证错误都会中断启动。
3. **SubagentManager**（子智能体管理器）：基于 `asyncio.Task` 实现并行子智能体执行，内置端点互斥机制（URL 归一化 + inflight 字典），通过 `_SubagentHook` 实时将工具调用事件广播到前端 WebSocket。

子智能体之间通过**黑板通信**（Blackboard）共享发现——每个条目自动提取 kind 标签（finding/milestone/blocker/progress），Orchestrator 在分发任务前读取黑板以避免重复工作；通过**资产推送**（AssetFeed）实现资产发现联动——支持 6 种标准资产类型（url/port/service/credential/vuln/tech），采用游标分页机制。

### 2.4 技能系统

技能系统是安全能力的载体，采用"Markdown 描述 + Python handler"的二层架构：

- **SKILL.md**：YAML frontmatter 定义元数据（名称、风险等级、外部依赖），Markdown body 描述用法，运行时注入 LLM 上下文。
- **handler.py**：实现 `async run(args, ctx) → SkillResult` 标准接口，返回结构化结果（摘要 + 发现 + CMDB 写入指令 + 原始日志）。

系统当前集成了 28+ 安全技能，覆盖 qscan、fscan、nuclei、sqlmap、katana、httpx、ffuf、hydra 等主流安全工具，以及手动漏洞检测、HTML 报告生成、Python 脚本执行等内置能力。

### 2.5 工作流引擎

工作流引擎（约 3300 行）独立于智能体对话流程运行，支持可视化编辑、定时触发和条件分支。架构分四层：

- **服务层**（WorkflowService）：门面模式，封装 CRUD、Cron 同步和 per-workflow 并发锁。
- **执行层**（WorkflowRunner）：步骤编排，包含 AST 安全表达式引擎的条件求值和 `${path}` 模板插值。
- **执行器层**：5 种执行器——Tool（安全技能）、Script（Python 子进程）、Agent（专家智能体）、LLM（直接调用）、LLM Chunked（分块处理）。
- **存储层**（WorkflowStore）：JSON + FileLock 原子写入，运行日志追加记录（上限 1000 条）。

内置两套工作流模板：钓鱼邮件检测（脚本特征提取 → LLM 智能判定 → 结果聚合 + rspamd 评分）和日志安全分析（文件读取 → 分块 LLM 分析 → 结果入库）。

### 2.6 前端 WebUI

前端基于 React 18 + Vite + TypeScript + Tailwind CSS + shadcn/ui 构建，包含 9 条路由、70+ UI 组件、9 个自定义 Hooks 和 14 个工具库。通过 WebSocket 与后端实时通信，支持 9 种事件类型（消息、流式增量、工具调用帧、智能体生命周期事件、进度、高危确认弹窗等），完整呈现 AI 的思考与执行过程。

---

## 三、系统技术优势

### 3.1 对话即调度，零流程图编排

传统安全自动化方案通常需要预先定义复杂的工作流 DAG（有向无环图），每次新增场景都要修改流程配置。secbot-ai 采用 LLM Function Calling 驱动的动态编排模式：Orchestrator 根据用户意图和当前上下文实时决策下一步动作，天然支持多轮对话、中途追问、异常恢复和动态调整。一次对话中，用户可以先说"探测 10.0.0.0/24"，然后说"对刚才发现的 HTTP 服务扫漏洞"，系统自动关联上下文完成接力。

### 3.2 专家智能体完全解耦，热插拔扩展

每个专家智能体是一个独立的 YAML 配置 + 提示词文件 + 绑定工具集的组合，彼此之间没有代码级依赖。AgentRegistry 的启动时全量验证确保任何配置错误在启动阶段即暴露，而非运行时崩溃。新增一个安全能力只需三步：创建提示词文件、编写 YAML 配置、重启系统——Orchestrator 的专家表自动更新，零代码侵入。

### 3.3 多层安全防护，合规可审计

系统在多个层面构建了安全防线：

| 防护层 | 机制 | 说明 |
|--------|------|------|
| **高危门控** | HighRiskGate | `critical` 级别技能执行前阻塞等待用户确认（120s 超时），同会话同技能批准后免重复弹窗 |
| **审计日志** | AuditLogger | 每次确认请求（approve/deny/timeout）完整记录，支持离线导出 |
| **SSRF 防护** | AgentRunner 内置检测 | 识别 `internal/private url` 标记，不可绕过的安全提示 |
| **工作区沙箱** | Workspace Restriction | 检测路径遍历和越权操作，重复违规自动升级提示 |
| **端点互斥** | URL 归一化 + inflight 字典 | 防止并发扫描同一目标端点，避免资源竞争和重复测试 |
| **表达式沙箱** | AST 白名单求值 | 工作流条件表达式无 eval/Call/dunder，杜绝代码注入风险 |

### 3.4 异步并行，高吞吐处理

基于 Python `asyncio` 的全异步架构，SubagentManager 通过 `asyncio.Task` 并行执行多个专家智能体，MessageBus 的双 `asyncio.Queue` 将通道层与智能体层完全解耦。AgentLoop 通过 Semaphore 控制并发上限（默认 3），配合 `pending_queue`（maxsize=20）实现中途消息注入——子智能体结果、资产发现、用户追问可以按优先级（user > subagent_result > asset_discovered）有序注入正在进行的对话循环。

### 3.5 多模型多通道，灵活部署

系统支持 6 种 LLM 提供商（Anthropic、OpenAI、Azure OpenAI、AWS Bedrock、GitHub Copilot、OpenAI Codex），运行时可热切换模型和 Provider，无需重启。通道层采用插件式架构，通过 `pkgutil` 扫描内置模块 + `entry_points` 外部插件自动发现，当前支持 WebUI、CLI、Telegram、Discord、Slack、钉钉、飞书、QQ 和 OpenAI 兼容 API 共 9 种接入方式。

### 3.6 上下文治理，长对话不衰减

AgentRunner 的 6 步上下文治理管线是处理长对话场景的核心：

1. **孤立结果清理**：清除无主 tool 结果消息
2. **缺失结果补缺**：补充遗漏的 tool 结果占位
3. **旧结果摘要化**：保留最近 10 条完整结果，更早的压缩为摘要
4. **超大结果截断**：按 token 预算截断过大的工具输出
5. **历史裁剪**：基于 token 预算裁剪过长的对话历史
6. **二次清理**：再次执行孤立/补缺确保一致性

配合 AutoCompact（自动压缩）和 Dream（记忆巩固）机制，即使在大规模扫描产生海量工具输出的场景下，也能保持上下文窗口的有效利用。

### 3.7 CMDB 统一建模，数据可追溯

所有扫描结果统一写入 CMDB（SQLite + SQLAlchemy + Alembic 迁移），涵盖 Scan → Asset → Service → Vulnerability → Report 完整数据链。支持自然键 upsert（IP > hostname > target 归一化匹配）、资产自动分类（根据端口/产品指纹推断类型）、关键漏洞实时通知。仪表盘提供 5 个 KPI 卡片、漏洞趋势分析、分类分布、业务系统聚类、资产-服务-漏洞拓扑等丰富的聚合查询能力。

---

## 四、系统解决的问题

### 4.1 安全运营效率瓶颈

**问题**：传统安全评估需要工程师手动操作 nmap、nuclei、sqlmap 等十余种工具，每种工具有不同的命令语法和输出格式，一次完整的 VAPT 评估往往需要数天甚至数周。

**解决**：secbot-ai 将完整的安全测试流水线（资产发现 → 端口扫描 → Web 爬取 → 漏洞预筛 → 漏洞扫描 → 弱口令检测 → 报告生成）自动化编排，用户只需一句对话即可启动全流程。Orchestrator 根据中间结果动态调整后续步骤，跳过不必要的环节，最大化执行效率。

### 4.2 安全工具碎片化

**问题**：安全团队通常需要维护一套复杂的工具链，工具之间缺乏统一的数据模型和通信协议，结果整合困难。

**解决**：技能系统将 28+ 安全工具统一封装为标准化的 SkillTool，每个技能拥有统一的 `SkillResult` 输出格式（摘要 + 发现 + CMDB 写入指令）。扫描结果自动写入 CMDB 统一存储，资产通过 AssetFeed 在智能体间实时流转，彻底消除信息孤岛。

### 4.3 高危操作风险管控

**问题**：渗透测试中的爆破、PoC 验证等操作具有高风险性，误操作可能导致业务中断或数据泄露，但传统工具缺乏细粒度的操作级审批机制。

**解决**：HighRiskGate 在工具执行层面实现了精确的风险分级管控。每个技能在 SKILL.md 中声明 `risk_level`，`critical` 级别的技能执行前自动触发确认弹窗（WebUI）或交互确认（CLI），120 秒超时自动拒绝。同一会话内同一技能批准后免重复弹窗，平衡了安全性与操作效率。所有确认记录由 AuditLogger 完整审计留痕。

### 4.4 AI 上下文窗口限制

**问题**：安全扫描产生的工具输出往往非常庞大（一次端口扫描可能返回数千行结果），轻易超出 LLM 的上下文窗口限制，导致信息丢失或对话中断。

**解决**：AgentRunner 的 6 步上下文治理管线 + microcompact 摘要化机制，将旧的工具输出自动压缩为关键摘要，保留最近的完整数据。配合 `apply_tool_result_budget` 截断超大结果、`snip_history` 基于 token 预算裁剪历史，确保即使面对大规模扫描输出也能维持有效的上下文窗口利用率。

### 4.5 多平台接入碎片化

**问题**：安全团队使用不同的协作工具（钉钉、飞书、Slack 等），为每个平台单独开发 Bot 适配成本高昂。

**解决**：通道层采用插件式架构 + MessageBus 双队列解耦，所有通道统一抽象为 `BaseChannel` 接口（start/stop/send/send_delta/is_allowed）。内置通道通过 `pkgutil` 自动发现，外部通道通过 `entry_points` 插件机制接入。每个通道独立配置 `allowFrom` 白名单和转录设置，真正实现"一次开发，多平台部署"。

### 4.6 安全评估结果不可追溯

**问题**：一次性扫描的结果散落在各种日志文件和终端输出中，缺乏统一的存储和查询手段，难以进行历史对比和合规审计。

**解决**：CMDB 提供了完整的持久化方案——从扫描任务（Scan）到资产（Asset）到端口服务（Service）到漏洞（Vulnerability）到报告（Report），全链路结构化存储。支持 ULID 时间排序、自然键去重、严重度分级、自动资产分类。仪表盘提供 24 小时增量 KPI、7/30/90 天漏洞趋势、资产风险拓扑等可视化分析能力。

---

## 五、开发路线图与未来功能

### Phase 1 — 核心能力巩固（当前阶段）

**目标**：夯实核心 VAPT 流水线，确保端到端流程的稳定性和可靠性。

- 完善现有 7 个专家智能体的提示词优化和工具覆盖
- 增强 CMDB 仪表盘的可视化能力（资产拓扑图、漏洞热力图）
- 优化上下文治理策略，提升大规模扫描场景下的对话质量
- 补充集成测试覆盖（agent/skills/workflow 模块）
- WebUI 交互体验打磨（消息渲染、工具调用卡片、智能体事件展示）

### Phase 2 — 能力扩展与生态建设

**目标**：扩展安全能力边界，构建开放的技能和通道生态。

- **新增专家智能体**：
  - 渗透测试智能体（pentest）：自动化漏洞利用验证
  - 合规检查智能体（compliance）：等保 / ISO 27001 / GDPR 合规评估
  - 威胁建模智能体（threat_model）：基于资产拓扑的攻击面分析
  - 应急响应智能体（incident_response）：安全事件自动化处置
- **新增安全技能**：
  - Masscan 大规模端口扫描
  - Burp Suite 集成（被动扫描 + 主动扫描）
  - Metasploit 漏洞利用框架对接
  - 自定义 PoC 执行引擎
- **通道生态**：
  - 企业微信通道
  - Microsoft Teams 通道
  - Webhook / 邮件通知通道

### Phase 3 — 智能化升级

**目标**：引入更高级的 AI 能力，提升自动化深度和决策质量。

- **多模型协作**：Orchestrator 使用强推理模型（如 Claude Opus），专家智能体使用快速模型（如 Claude Haiku），在推理质量和执行速度之间取得平衡
- **RAG 安全知识库**：集成 CVE 数据库、Exploit-DB、安全公告等外部知识源，为漏洞判定和修复建议提供实时参考
- **自适应扫描策略**：基于历史扫描数据和资产变更自动调整扫描范围和深度，减少重复扫描
- **智能报告生成**：根据受众（技术人员 / 管理层 / 合规审计）自动生成不同粒度和风格的报告
- **多轮推理增强**：引入 Chain-of-Thought 可视化，让用户看到 Orchestrator 的完整决策链路

### Phase 4 — 企业级能力

**目标**：满足企业级安全运营的规模化、协作化和合规化需求。

- **多租户支持**：团队隔离、权限管理、资源配额
- **协作扫描**：多名安全工程师协同参与同一评估任务，任务分配与进度同步
- **持续安全监控**：与 CMDB 深度集成，支持资产变更自动触发增量扫描
- **CI/CD 集成**：提供 API 和 CLI 工具，将安全扫描嵌入 DevOps 流水线
- **分布式部署**：支持多节点扫描代理，适应大规模网络和跨区域环境
- **合规报告模板**：等保测评报告、渗透测试报告、风险评估报告等标准化模板
- **数据加密与访问控制**：扫描结果加密存储、细粒度数据访问策略、操作审计与回溯

### Phase 5 — 平台化演进

**目标**：从工具平台演进为安全运营中枢。

- **安全编排自动化与响应（SOAR）**：将工作流引擎升级为完整的 SOAR 平台，支持 Playbook 编排、事件响应自动化
- **威胁情报集成**：对接主流威胁情报源（VirusTotal、AlienVault OTX、微步在线），实现 IOC 自动匹配和上下文丰富
- **AI 红队模拟**：基于 ATT&CK 框架的自动化红队演练，模拟真实攻击路径
- **安全知识图谱**：构建资产-服务-漏洞-攻击向量-修复方案的知识图谱，支持智能问答和决策辅助
- **插件市场**：开放技能市场，支持社区贡献和分发的安全技能插件
- **多云适配**：AWS / Azure / GCP / 阿里云 / 腾讯云原生集成，支持云资产自动发现和安全评估

---

## 附录：技术栈速查

| 层级 | 技术选型 |
|------|----------|
| 后端语言 | Python 3.11+ |
| 前端语言 | TypeScript |
| 包管理 | Hatch / uv |
| LLM 提供商 | Anthropic、OpenAI、Azure OpenAI、AWS Bedrock、GitHub Copilot、OpenAI Codex |
| Web 框架 | aiohttp（API 服务）、websockets（WebSocket 通道） |
| 前端框架 | React 18 + Vite + Tailwind CSS + shadcn/ui |
| 数据库 | SQLite + SQLAlchemy 2.x (async) + Alembic |
| CLI | Typer |
| 消息协议 | OpenAI-compatible `/v1/chat/completions` |
| 安全工具 | qscan、fscan、nuclei、sqlmap、katana、httpx、ffuf、hydra |
| 工作流存储 | JSON + FileLock 原子写入 |
| 表达式引擎 | Python AST 白名单沙箱 |
