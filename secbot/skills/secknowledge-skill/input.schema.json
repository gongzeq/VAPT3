{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["query"],
  "additionalProperties": false,
  "properties": {
    "query": {
      "type": "string",
      "minLength": 2,
      "maxLength": 500,
      "description": "Security-testing question, target trait, vulnerability class, payload family, or methodology keyword to look up in the bundled references."
    },
    "category": {
      "type": "string",
      "enum": ["auto", "web", "ai", "payloads", "methodology", "all"],
      "default": "auto",
      "description": "Reference family to search. Use auto unless the task is clearly Web, AI, payload, or methodology focused."
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "default": 5,
      "description": "Maximum reference excerpts to return."
    }
  }
}
