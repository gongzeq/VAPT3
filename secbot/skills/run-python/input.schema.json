{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["code"],
  "additionalProperties": false,
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32768,
      "description": "Python 3 source code to execute. Runs once, no arguments, no stdin. Limit 32KB."
    },
    "timeout_sec": {
      "type": "integer",
      "minimum": 1,
      "maximum": 600,
      "default": 60,
      "description": "Wall-clock timeout in seconds. Interpreter is terminated (SIGTERM then SIGKILL) when exceeded."
    }
  }
}
