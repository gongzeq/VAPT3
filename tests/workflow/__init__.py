"""Workflow engine tests — foundation layer.

Covers the types, template/expression helpers and disk store. Runner and
executor tests live alongside ``secbot/workflow/runner.py`` once that
module lands.
"""
